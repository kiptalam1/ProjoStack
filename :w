export default function CreateWorkspaceForm() {


  return (
    <div>
      <form className="max-w-md w-full sm:w-sm md:w-md bg-card shadow-sm shadow-gray-200 rounded-lg p-4">
        <label className="text-sm">Workspace name</label>
        <input type="text"
          className="border border-border rounded-lg outline-none focus:ring focus:ring-primary" />
        <div className="flex items-center justify-between gap-3">
          <button>Cancel</button>
          <button>Save</button>
        </div>
      </form>
    </div>
  )
}
